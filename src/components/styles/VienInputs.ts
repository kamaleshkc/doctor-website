import styled from "styled-components";
import { Input } from "antd";

const VienInput = styled(Input)`
  border-radius: 14px;
  padding: 1rem;
`;

export const VienPassword = styled(Input.Password)`
  border-radius: 14px;
  padding: 1rem;
`;

export default VienInput;
